#pragma once

// Copy this file to secrets.h (gitignored) in this same folder and fill in
// your WiFi credentials. Never commit secrets.h.

#define WIFI_SSID     "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"

// MQTT broker — matches agent_tools/mqtt.py / .env on the Python side.
#define MQTT_BROKER_HOST "crystalmq.bevywise.com"
#define MQTT_BROKER_PORT 1883
#define MQTT_USERNAME     "your-mqtt-username"
#define MQTT_PASSWORD     "your-mqtt-password"
#define MQTT_TOPIC        "jobhunter/notifications"
